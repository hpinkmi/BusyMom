@model IEnumerable<BusinessLogicLayer.UsersBLL>

    <p>
        @if (User.IsInRole(BusyMomWeb.MagicConstants.ParentAboveName))
        {
            @Html.ActionLink("Create New", "Create")
        }
    </p>
    <p>
        @if (User.IsInRole(BusyMomWeb.MagicConstants.ParentAboveName))
        {
            @Html.ActionLink("show Stats", "MeaningfulCalculation")
        }
    </p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UserID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LastName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Phone)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UserName)
        </th>
        @*<th>
            @Html.DisplayNameFor(model => model.Hash)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Salt)
        </th>
        <th></th>*@
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.UserID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LastName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Email)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Phone)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UserName)
        </td>
        <td>
            @*@Html.DisplayFor(modelItem => item.Hash)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Salt)
        </td>
        <td>*@
            @if (User.IsInRole(BusyMomWeb.MagicConstants.ParentAboveName))
            {
                @Html.ActionLink("Edit", "Edit", new { id = item.UserID }) <text>|</text>
            }
            @Html.ActionLink("Details", "Details", new { id = item.UserID }) |
            @if (User.IsInRole(BusyMomWeb.MagicConstants.ParentAboveName))
            {
                @Html.ActionLink("Delete", "Delete", new { id = item.UserID })
                <text>|</text>
            }
            @Html.ActionLink("Groups", "Groups", new { id = item.UserID })
        </td>
    </tr>
}

</table>
